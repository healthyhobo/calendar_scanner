pandas>=2.0
numpy>=1.24
requests>=2.28
pyyaml>=6.0
pyarrow>=12.0
matplotlib>=3.7
scipy>=1.10           # required for Black-Scholes pricing (scipy.stats.norm)
tabulate>=0.9
ib_insync>=0.9.86
tqdm>=4.65
python-dotenv>=1.0.0
numba>=0.57           # optional but strongly recommended: 50x faster rolling percentile
                      # install with: pip install numba
                      # falls back to pure numpy if not present
